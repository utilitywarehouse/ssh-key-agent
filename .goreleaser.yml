version: 2
builds:
  - id: main
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
archives:
  - id: main
    ids:
      - main
    formats:
      - binary
    files:
      - none*
release:
  github:
    owner: utilitywarehouse
    name: ssh-key-agent
